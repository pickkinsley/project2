version: "2"
sql:
  - engine: "mysql"
    queries: "queries/*.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_interface: true
        emit_json_tags: true
